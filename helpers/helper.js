/*
================================================================
 Helper the class
================================================================
 Class with a helpful functions
================================================================
*/

const helper = {
    /*
    ================================================================
     Get string abd make it first symbol is uppercase
    ================================================================
     >>> string [str] : input string >>>
    ================================================================
     <<< string <<<
    ================================================================
    */
    ucFirst : (str) => {
        return str[0].toUpperCase() + str.substring(1);
    }
};

module.exports = helper;